### Exercise 7: Create a symlink using a new environemnt variable

Create a symlink in your `$HOME` directory to `$SI26_DATA_DIR`. This will a useful shorthand reference to many of the files you will work with next week.

*Command*
```
ln -s $SI26_DATA_DIR sdsc-data
```

*Output*
```
[mkandes@login01 ~]$ ln -s $SI26_DATA_DIR sdsc-data
[mkandes@login01 ~]$
```
